import { Card } from "react-bootstrap";

const Settings = () => (
  <Card className="shadow-sm">
    <Card.Body>
      <Card.Title>Settings</Card.Title>
      <p>This is where admin settings would go.</p>
    </Card.Body>
  </Card>
);

export default Settings;